class User {
  constructor(id, name, groupIds, imageUri, color) {
    this.id = id;
    this.name = name;
    this.groupIds = groupIds;
    this.imageUri = imageUri;
    this.color = color;
  }
}

export default User;
